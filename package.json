{
  "name": "data-deserts",
  "private": true,
  "scripts": {
    "build": "npm run build:data && npm run build:app",
    "build:app": "node scripts/build-app.mjs",
    "build:data": "python3 processing/build_data.py",
    "fetch:all": "PYTHON_BIN=python3 bash processing/fetch_all.sh",
    "deploy:vercel": "node --env-file=.env scripts/deploy-app-vercel.mjs",
    "build:do-wall": "node scripts/build-do-wall.mjs"
  },
  "devDependencies": {
    "archiver": "^8.0.0",
    "esbuild": "^0.25.0"
  }
}
